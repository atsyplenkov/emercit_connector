[project]
name = "emercit-connector"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "requests",
  "pytz",
  "pymongo",
  "tqdm",
  "retrying",
  "pandas!=1.3.0",
]

[dependency-groups]
dev = [
  "pylint",
]

[tool.uv]
default-groups = ["dev"]
